import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.monacetracker',
  appName: 'MonaceTracker',
  webDir: 'dist'
};

export default config;
